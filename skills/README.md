# skills/

This folder holds the agent skills this project publishes for other coding agents to install. `local-build/` is
the only one: the front door an agent uses to dispatch a brief to the seat on the Arc A770, described in
`skills/local-build/SKILL.md`.

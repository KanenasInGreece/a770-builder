# config/

This folder holds the project's configuration: the registries that list every qualified model and its measured
numbers, one per card mode; the example environment file a new install copies and edits; and the template opencode
reads inside the sandbox. `docs/OPERATING.md` explains how these layer together and which setting lives where.
